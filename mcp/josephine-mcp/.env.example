# Required
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: use anon key instead (RLS applies, writes may fail)
# SUPABASE_ANON_KEY=your-anon-key-here

# Write safety gate (default: false)
# Set to "true" to enable write operations
JOSEPHINE_MCP_WRITE_ENABLED=false

# Bulk-cap: max rows affected per single write request (default: 20000)
# JOSEPHINE_MCP_MAX_ROWS_PER_WRITE=20000

# Circuit breaker: trips after N errors within WINDOW, then preview-only for COOLDOWN
# JOSEPHINE_MCP_BREAKER_THRESHOLD=10
# JOSEPHINE_MCP_BREAKER_WINDOW_SEC=60
# JOSEPHINE_MCP_BREAKER_COOLDOWN_SEC=60
