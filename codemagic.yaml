# ──────────────────────────────────────────────────────────
# Codemagic CI/CD – Josephine iOS + iPad Universal Build
# ──────────────────────────────────────────────────────────
# Docs: https://docs.codemagic.io/yaml-quick-start/building-a-capacitor-app/
#
# Two workflows:
#   1. ios-verify  → Quick build WITHOUT signing (free, no Apple account needed)
#   2. ios-release → Full build WITH signing + TestFlight (requires Apple Dev Account)
#
# Triggers: push to main branch, or manual run from Codemagic dashboard.
# ──────────────────────────────────────────────────────────

workflows:

  # ═══════════════════════════════════════════════
  # WORKFLOW 1: Verify build (NO signing required)
  # ═══════════════════════════════════════════════
  # Use this now — verifies npm + Capacitor + Xcode compile without Apple account
  ios-verify:
    name: Josephine iOS Verify (No Signing)
    max_build_duration: 30
    instance_type: mac_mini_m2

    environment:
      xcode: latest
      node: 22
      cocoapods: default
      vars:
        BUNDLE_ID: app.josephine.dashboard
        APP_NAME: Josephine

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      cancel_previous_builds: true

    scripts:
      # ── 1. Install Node dependencies ──
      - name: Install dependencies
        script: |
          npm ci

      # ── 2. Build web app → dist/ ──
      - name: Build web app
        script: |
          npm run build

      # ── 3. Sync iOS platform ──
      - name: Sync iOS platform
        script: |
          npx cap sync ios

      # ── 4. Install CocoaPods ──
      - name: Install CocoaPods
        script: |
          cd ios/App && pod install

      # ── 5. Build without signing (verify compilation) ──
      - name: Build iOS (no signing)
        script: |
          xcodebuild \
            -workspace "ios/App/App.xcworkspace" \
            -scheme "App" \
            -configuration "Release" \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            PROVISIONING_PROFILE="" \
            | xcpretty

    artifacts:
      - /tmp/xcodebuild_logs/*.log

    publishing:
      email:
        recipients:
          - oscar.tejera99@gmail.com
        notify:
          success: true
          failure: true

  # ═══════════════════════════════════════════════
  # WORKFLOW 2: Full release (Apple Dev Account required)
  # ═══════════════════════════════════════════════
  # Uncomment this entire block when you have Apple Developer Account ($99/yr)
  #
  # ios-release:
  #   name: Josephine iOS Release
  #   max_build_duration: 30
  #   instance_type: mac_mini_m2
  #
  #   environment:
  #     xcode: latest
  #     node: 20
  #     cocoapods: default
  #     groups:
  #       - app_store_credentials
  #     ios_signing:
  #       distribution_type: app_store
  #       bundle_identifier: app.josephine.dashboard
  #     vars:
  #       BUNDLE_ID: app.josephine.dashboard
  #       APP_NAME: Josephine
  #
  #   scripts:
  #     - name: Install dependencies
  #       script: npm ci
  #     - name: Build web app
  #       script: npm run build
  #     - name: Sync iOS platform
  #       script: npx cap sync ios
  #     - name: Generate app icons
  #       script: |
  #         if [ -f resources/icon.png ]; then
  #           npx capacitor-assets generate --iconBackgroundColor '#7c3aed' --splashBackgroundColor '#0d1117' --ios
  #         fi
  #     - name: Install CocoaPods
  #       script: cd ios/App && pod install
  #     - name: Set build version
  #       script: |
  #         cd ios/App
  #         agvtool new-version -all $(($(app-store-connect get-latest-testflight-build-number "$BUNDLE_ID" || echo 0) + 1))
  #     - name: Build iOS
  #       script: |
  #         xcode-project build-ipa \
  #           --workspace "ios/App/App.xcworkspace" \
  #           --scheme "App" \
  #           --config "Release"
  #
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #     - /tmp/xcodebuild_logs/*.log
  #
  #   publishing:
  #     email:
  #       recipients:
  #         - oscar.tejera99@gmail.com
  #       notify:
  #         success: true
  #         failure: true
  #     app_store_connect:
  #       auth: integration
  #       submit_to_testflight: true
